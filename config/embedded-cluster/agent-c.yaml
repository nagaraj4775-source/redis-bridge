site_id: "ec-site-c"

cluster:
  mode: "cluster"
  masters:
    - "redis-c-master1:6379"
    - "redis-c-master2:6379"
    - "redis-c-master3:6379"
  password: ""
  tls: false

bus:
  mode: "embedded"          # stream lives on redis-c-master1 (master_index: 0)
  master_index: 0
  peer_bus_addrs:           # reach each peer's stream on their master1
    ec-site-a: "redis-a-master1:6379"
    ec-site-b: "redis-b-master1:6379"
  stream_prefix: "repl:stream:"
  consumer_group: "repl-consumers"
  password: ""

peers:
  - "ec-site-a"
  - "ec-site-b"

replication:
  batch_size: 100
  apply_concurrency: 8
  dedup_ttl_seconds: 5
  max_in_flight: 1000

coordinator:
  port: 8080

metrics:
  port: 9090
