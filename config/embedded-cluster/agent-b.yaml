site_id: "ec-site-b"

cluster:
  mode: "cluster"
  masters:
    - "redis-b-master1:6379"
    - "redis-b-master2:6379"
    - "redis-b-master3:6379"
  password: ""
  tls: false

bus:
  mode: "embedded"          # stream lives on redis-b-master1 (master_index: 0)
  master_index: 0
  peer_bus_addrs:           # reach each peer's stream on their master1
    ec-site-a: "redis-a-master1:6379"
    ec-site-c: "redis-c-master1:6379"
  stream_prefix: "repl:stream:"
  consumer_group: "repl-consumers"
  password: ""

peers:
  - "ec-site-a"
  - "ec-site-c"

replication:
  batch_size: 100
  apply_concurrency: 8
  dedup_ttl_seconds: 5
  max_in_flight: 1000

coordinator:
  port: 8080

metrics:
  port: 9090
